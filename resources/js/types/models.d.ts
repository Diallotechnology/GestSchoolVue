export interface Users {
    id: number;
    name: string;
    email: string;
}

export interface Service {
    id: number;
    name: string;
}

export interface Client {
    id: number;
    nom: string;
    prenom: string;
    contact: string;
}

